# v1.1.1 发布验证报告

源码和 Windows 软件对应 GitHub 标签 `v1.1.1`。应用版本为 1.1.1，Windows 数字文件版本为 1.1.1.0，不带预发布标志。

## 使用与变化

完整解压 `12306FairTicket-1.1.1-Windows-x64.zip`，保留 EXE 同级 `_internal` 目录。按 **调参数 → 开始任务 → 扫二维码 → 等待** 使用。程序不再弹出 Windows 系统通知或播放提示音，请留意窗口中的状态、日志及结果提示框。

卧铺菜单包含高级软卧、软卧、硬卧、一等卧、二等卧，仍为三列布局，初始不勾选任何席别。旧配置中的软卧、硬卧不会自动转换；如同时接受普通卧铺与一等卧／二等卧，请分别勾选并调整优先顺序。保存仍使用 JSON v3，兼容导入 v1/v2/v3。

## 卧铺代码依据与处理

核对 12306 官方公开脚本：一等卧代码为 I，二等卧为 J；软卧为 4、硬卧为 3。虽然共享查询结果的余票列（零基下标 23、28），却必须使用各自的订单代码。查询解析新增保留零基下标 35 的实际席别代码，通过它区分这两组卧铺，不依据车次前缀猜测。

代码缺失或同一列含多个无法区分的席别时，只跳过该列对应的卧铺候选，每次任务对每趟车、每个共享列至多记录一次警告。其他可识别的席别继续正常筛选。选铺和中铺支持仍依据服务端响应，能力不足时保留现有偏好降级提示。

官方依据（2026-09-16 只读核对）：

- [席别代码及名称](https://kyfw.12306.cn/otn/resources/merged/common_js.js?scriptVersion=1.95051)
- [查询结果字段解析与共享列](https://kyfw.12306.cn/otn/resources/merged/queryLeftTicket_end_js.js?scriptVersion=1.95051)

## 测试结果

- 完整回归：**433 passed，13 subtests passed**。包含登录复用、停止／重启、旧配置导入、多分隔符及两种尝试策略。
- 卧铺专项：54 项测试，覆盖代码区分、缺失／歧义数据、去重日志、无票及从查询解析到订单提交的 I/J 载荷；同时验证铺位能力与中铺降级。
- 150% GUI 回归：**209 passed**。全部 12 种席别点击、键盘切换、三列布局和模型拖动排序覆盖通过。
- 系统提醒：使用通知／提示音调用监测验证重复发现票源、扫码、登录、失败和订单成功，确认系统提醒为零，程序内结果提示框保留。
- 深浅主题分别检查 100%／150% 缩放，一等卧与二等卧、已选优先顺序均可正常显示，初始零勾选。
- Windows EXE 通过构建脚本的离线启动检查；独立启动、TLS 依赖检查见随包 `PORTABLE-CHECK.json`，来源和文件哈希见 `BUILD-INFO.json`。

测试只使用模拟数据，没有登录 12306 或提交真实订单。拖动覆盖 Qt 事件和模型重排，Windows OLE 原生拖放未进行端到端验证。1200×720 最小窗口下，原有状态指标区仍存在文字压缩，席别区域可滚动访问；本轮没有改变该状态区布局。

本地记录：`artifacts/v111-tests.txt`、`artifacts/v111-tests.xml`、`artifacts/v111-scale150.txt`；界面截图：`artifacts/v111-{light,dark}-{100,150}`。
