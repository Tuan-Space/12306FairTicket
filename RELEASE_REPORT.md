# 1.1.0 发布验证报告

本版本包含车次与席别优先策略、席别界面简化及三列布局。源码和 Windows 便携软件包对应 GitHub 标签 `v1.1.0`。

本次修复停止任务、修改配置后再次启动的登录显示：后台验证会话有效后同步通知界面，二维码区域显示“已登录，无需重新扫码”；失效会话仍走扫码流程。README 顶部列出四步使用流程。

## 使用

完整解压 `12306FairTicket-Windows-x64.zip`，运行目录内的 `12306FairTicket.exe`，保留同级 `_internal` 文件夹。无需另行安装 Python。EXE 和界面版本均标记为 `1.1.0`，Windows 数字文件版本为 `1.1.0.0`，不带预发布标志。

1. 填写行程和乘车人。优先车次可以留空，留空时不限类型，按已选席别查询。
2. 初始不勾选任何席别。在“坐席”“卧铺”两个三列菜单中选择接受的席别，再到三列的“已选席别与优先顺序”调整排序。
3. 点击“尝试策略”的右侧箭头或文字区域，选择“车次优先”或“席别优先”；也可使用 Alt+↓ 和方向键、回车选择。
4. 需要时填写座位或铺位偏好；标注“未启用”的偏好仅保留为草稿。
5. 保存配置或检查配置。配置保存为 v3，支持导入 v1/v2/v3；旧版 v1.0.1 软件不能导入 v3 文件。

两个菜单默认展开，输入车次不会改变菜单展开状态或已选席别。坐席共 7 项（含无座），卧铺共 3 项；动车卧铺继续保留。GUI 已删除“未指定车次时的范围”，保存和构建任务时将其兼容字段固定为 `all`。导入旧预览版的其他范围会转换并在现有导入提示中说明，CLI 范围参数保持有效。更完整的规则与示例见 `README_GUI.md`。

## 验证

- 登录重启回归使用真实工作线程、客户端登录流程和 Qt 信号，覆盖首次扫码 → 停止 → 修改车次与席别 → 有效会话复用 → 会话失效后重新扫码；也验证检查期间停止不会继续登录。网络响应使用模拟数据。
- 完整测试：**356 passed，13 subtests passed**。覆盖全部席别的三列位置和点击、初始零勾选、范围移除与旧配置转换，以及原有车次分类、排序、偏好、JSON 和 CLI 回归。
- 150% 缩放界面回归：**192 passed**，包括主窗口、席别控件、鼠标与键盘事件、跨行拖放，以及下拉箭头渲染和菜单交互。
- Windows 原生 Qt 平台在进程缩放 1.0 和 1.5 下分别检查深浅主题，保存初始零勾选、三列布局、下拉箭头、混合配置及车次留空截图。没有更改系统缩放设置。
- 订单测试使用模拟响应，覆盖座卧混选的实际偏好载荷及服务端能力不足时的真实降级提示；没有登录 12306 或提交真实订单。
- 拖放覆盖 Qt 鼠标事件、拖动阈值、MIME、dropEvent 与模型重排；Windows OLE 原生拖放循环仍未完成端到端验证。

本地测试记录位于 `artifacts/release-110-login-tests.txt`、`artifacts/release-110-login-scale150.txt`；布局截图位于 `artifacts/dev2-native-*`，本次登录提示的深浅主题 150% 截图位于 `artifacts/login-fix-*-150`。正式版便携包的启动与依赖检查结果随包提供于 `PORTABLE-CHECK.json`，构建来源与文件哈希见 `BUILD-INFO.json`。
